

def person_lister(f):
    def inner(people):
        # Sort by age (index 2), converting to int for numerical comparison
        return map(f, sorted(people, key=lambda x: int(x[2])))
    return inner


